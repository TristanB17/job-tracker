All Jobs for <strong><%= @company.name %></strong>

<ul>
  <% @company.jobs.each do |job| %>
    <div id="job-<%= job.id %>"
      <li><%= job.title %></li>
      <li>Is this even working??????</li>
      <li><%= link_to "Edit", edit_job_path(job) %> </li>
      <li><%= link_to "Delete", job_path(job), method: 'delete', id: "delete-#{job.id}" %> </li>
    </div>
  <% end %>
</ul>
